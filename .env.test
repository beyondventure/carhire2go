# .env.test template for Backend Smoke Tests
# Copy this file to .env.test.local if you want to override locally, or just fill this out.
# For QA-01 tests to run, we need a service role key to bypass RLS for data seeding.
# SUPABASE_SERVICE_ROLE_KEY=insert_service_role_key_here
